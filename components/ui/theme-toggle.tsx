"use client"

import { Sun } from "lucide-react"
import { Button } from "@/components/ui/button"

// Simplified ThemeToggle that does nothing (light mode only)
export function ThemeToggle() {
  // Empty component to avoid breaking imports
  return null
}